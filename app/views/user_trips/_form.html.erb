<%= form_with(model: user_trip, local: true) do |form| %>
  <% if user_trip.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(user_trip.errors.count, "error") %> prohibited this user_trip from being saved:</h2>

      <ul>
        <% user_trip.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :user_id %>
    <%= form.text_field :user_id %>
  </div>

  <div class="field">
    <%= form.label :trip_id %>
    <%= form.text_field :trip_id %>
  </div>

  <div class="field">
    <%= form.label :attended %>
    <%= form.check_box :attended %>
  </div>

  <div class="field">
    <%= form.label :favorite %>
    <%= form.check_box :favorite %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
